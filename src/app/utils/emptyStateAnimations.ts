/**
 * Shared CSS keyframe animations for empty state illustrations.
 * Keyframes are now defined in /src/styles/index.css to avoid CSP violations.
 * This file is kept as a no-op to avoid breaking existing imports.
 */

export function injectEmptyStateCSS(): void {
  // No-op: keyframes moved to /src/styles/index.css
}

// Auto-inject on import (no-op — already in CSS)
injectEmptyStateCSS();
